class Node {
    // Constructor to initialize a node with data
    constructor(data) {
        // Value of the node
        this.data = data;        
        // Pointer to the next node
        this.next = null;      
        // Pointer to the previous node
        this.back = null;      
    }
}
